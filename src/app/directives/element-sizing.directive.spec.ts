import { ElementSizingDirective } from './element-sizing.directive';

describe('ElementSizingDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementSizingDirective();
    expect(directive).toBeTruthy();
  });
});
