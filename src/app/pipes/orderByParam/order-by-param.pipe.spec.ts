import { OrderByParamPipe } from './order-by-param.pipe';

describe('OrderByParamPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByParamPipe();
    expect(pipe).toBeTruthy();
  });
});
